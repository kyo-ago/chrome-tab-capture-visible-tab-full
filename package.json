{
  "name": "chrome-tab-captureVisibleTab-full",
  "description": "Get the full size capture at chrome.tabs.captureVisibleTab.",
  "version": "0.0.3",
  "author": "@kyo-ago",
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "stage": 1
        }
      ]
    ]
  },
  "dependencies": {
    "bluebird": "^2.9.24"
  },
  "devDependencies": {
    "babel-plugin-espower": "^0.2.2",
    "babelify": "^6.0.2",
    "browserify": "^10.0.0",
    "david": "^6.1.6",
    "fixpack": "^2.2.0",
    "mocha": "^2.2.4",
    "power-assert": "^0.11.0",
    "watchify": "^3.2.0"
  },
  "keywords": [
    "captureVisibleTab",
    "chrome",
    "extension"
  ],
  "license": "MIT",
  "main": "dist/index.js",
  "mainSrc": "src/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/kyo-ago/chrome-tab-captureVisibleTab-full.git"
  },
  "scripts": {
    "build": "browserify $npm_package_mainSrc -o $npm_package_main",
    "clean": "rm -fr node_modules dist/*",
    "deploy": "npm run fix && npm run clean && npm install && npm run build && npm version patch",
    "fix": "fixpack && david",
    "test": "mocha --require ./test/_base.js ./test/index.js",
    "watch": "watchify $npm_package_mainSrc -o $npm_package_main -v"
  }
}
